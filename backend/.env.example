# App settings
PORT=4000
NODE_ENV=development   # change to "production" on EC2

# PostgreSQL connection
# Format: postgres://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgres://youruser:yourpassword@localhost:5432/weatherdb

# JWT settings
JWT_SECRET=replace_with_a_strong_random_secret
JWT_EXPIRES_IN=7d